twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'

    globals:
        link_service: '@App\Service\LinkService'
        indicator: '@App\Service\IndicatorService'
