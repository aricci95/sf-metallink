{% if users%}
    {% for user in users %}
        {% include 'user/item.html.twig' with {'user' : user } %}
    {% endfor %}
{% else %}
    Aucun utilisateur trouvé pour les paramètres fournis.
{% endif %}