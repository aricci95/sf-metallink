<a href="{{ path('user_show', {'id': user.id }) }}" >
    <div class="divElementSmall">
        <div class="divPhoto" style="background-image:url({{ user.defaultPicture.path | imagine_filter('thumbnail') }});background-position: top center;background-size:cover;">
            <img class="pictoStatus" src="/images/icone/{{ user.statusPicture }}" />
        </div>
        <span class="userFont" style="color:{% if user.gender == 1 %}#3333CC{% else %}#CC0000{% endif %}">
            {{ user.username }}
        </span>
    </div>
</a>